import { HopeThemeConfig } from "@hope-ui/solid";

const hopeConfig: HopeThemeConfig = {
  initialColorMode: "system",
  lightTheme: {
    colors: {},
  },
  darkTheme: {
    colors: {},
  },
};

export { hopeConfig };
